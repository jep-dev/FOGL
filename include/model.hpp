#ifndef MODEL_HPP
#define MODEL_HPP

namespace Model {
	namespace Ply {
		/** @brief A structure representing a PLY primitive data type */
		struct Primitive;
		/** @brief A structure representing instances of a single property */
		struct Property;
		/** @brief A vector/stack of raw bytes (needs replacement) */
		struct Buffer;
		/** @brief A grouping of properties */
		struct Element;
		/** @brief A grouping of declarations */
		struct Header;
	} // --> 
}

#endif
